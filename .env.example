# Environment Variables

# Supabase Configuration
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
## Notes
# - Only use the public ANON key in the frontend. Never expose the SERVICE_ROLE key.
# - In Vite, only variables prefixed with VITE_ are exposed to the browser.
# - Set real values in a local `.env` file. Do not commit `.env`.
